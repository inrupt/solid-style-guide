version: 0.2

phases:
  install:
    commands:
      - echo "Version alpha.00002"
      - pwd
      - ls -alrt
      - env
      - echo "$application"
      - echo "${deploy-key}"
  build:
    commands:
      - echo "skipping build initially"
  post_build:
    commands:
      - echo "skipping install initially"

